
A simple script to fix old sample cds. It automatically finds left (`-L`) and right (`-R`) mono files, merges them into standard stereo WAVs and then deletes the rest.


## Requirements
You just need Python and numpy, soundfile
```bash
pip install numpy soundfile
